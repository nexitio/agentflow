/**
 * packages/nodes — node registry (AGENTS.md §5).
 *
 * Phase 2 (docs/project-plan.md) replaces this placeholder with the registry
 * for Trigger / Agent / Sub-node (Model, Memory, Knowledge, Tool) / Action /
 * Logic nodes. Every node ships six pieces: definition, runtime, migrations
 * stub, fixtures, tests, icon. Definitions are UI-only; runtimes are
 * worker-only — the split is enforced by design.
 */
export const nodeRegistryRevision = 0;
