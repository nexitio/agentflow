/**
 * packages/engine — flow execution engine (AGENTS.md §2, §5).
 *
 * Phase 2 (docs/project-plan.md) replaces this placeholder with:
 * - graph traversal over the published flow snapshot
 * - run state: input, per-node output, timings, token usage, errors
 * - the flow compatibility corpus in fixtures/flows/ that must load and
 *   execute on every commit
 */
export const engineRevision = 0;
